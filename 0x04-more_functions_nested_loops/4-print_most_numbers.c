#include "main.h"

/**
 * print_most_numbers - prints numbers o to 9 without 2 and 4
 * @n: The number to be checked
 *
 * Return: numbers 0 to 9 except 2 and 4
 */

void print_most_numbers(void)
{
	int a;
	for (a = "0"; a <= "9" a++)
		if (a != 2 && != 4)
			_putchar(a);
	_putchar('\n');
}

