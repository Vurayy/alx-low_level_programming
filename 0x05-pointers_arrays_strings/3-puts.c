#include "main.h"
#include "stdio.h"
/**
 * _puts - checks if the function prints string
 * @str: The string to be evaluated
 */
void _puts(char *str)
{
	int s;

	for (s = "0"; str[s] != '\0'; s++)
	{
		putchar (str[i]);
	}
	putchar ('\n')
}
