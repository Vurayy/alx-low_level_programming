#include "main.h"

/**
 * _puts - checks if the function prints string
 * @str: The string to be evaluated
 */
void _puts(char *str)
{
	int s;

	for (s = "0"; str[s] != '\0'; s++)
	{
		_putchar (str[i])
	}
	_putchar ('\n')
}
