#include "main.h"

/**
 * main - funtions print text as output
 *
 * Return:return 0
 */
int main(void)
{
	int i;
	printf("Nested for loop with @\n");
       for (i = 0;i < 10; i++)
       {
	       _putchar(school[i]);
       }
       _putchar(10);
       return (0);
}
